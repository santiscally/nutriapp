# Secretos — NUNCA commitear
.env
# Copias que deja scripts/rename-db.sh (y las manuales): traen TODOS los secretos de prod.
.env.bak-*
*.env.txt
*.env.local

# Material confidencial del cliente (contrato/presupuesto firmado): no va al repo público
presupuesto_nutriapp.pdf

# Maestro de artículos de TBC: además de las 9 columnas que usa nutriapp trae costos, márgenes,
# comisiones y precios por proveedor de 2225 artículos. El análisis (sin números) va en
# instrucciones_claude/07-maestro-articulos-y-catalogo.md; el archivo crudo se comparte aparte.
instrucciones_claude/maestro-articulos-tbc-*.xlsx

# Java / Maven
backend/target/
*.class
*.jar
!backend/.mvn/wrapper/*.jar

# Node / Vite (frontend)
frontend/node_modules/
frontend/dist/
# Bundles anteriores conservados como rollback durante un deploy.
frontend/dist-old-*/
frontend/dist-new/
frontend/.env

# IDEs
.idea/
*.iml
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Backups de DB (scripts/backup-db.sh) — datos, no van al repo
backups/
